/*
import 'package:flutter/material.dart';

class MultipleTabModal extends StatelessWidget {
  final List<Widget> children;
  final TabController tabController;
  final double? height;

  const MultipleTabModal({super.key, required this.tabController, this.height, required this.children});

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: height ?? 300,
      child: TabBarView(
        controller: tabController,
        physics: const NeverScrollableScrollPhysics(),
        children: children,
      ),
    );
  }
}
*/
